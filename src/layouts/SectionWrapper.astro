---
import { cn } from "@/lib/shadcnUtils";

// SectionWrapper - wraps content within sections with consistent max-width and padding

const { className } = Astro.props;
---

<div class={cn("relative z-10 mx-auto container px-8 py-32", className)}>
	<slot />
</div>
